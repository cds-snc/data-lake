name: Get version to deploy
description: Resolve and output the version to deploy for tagged and release PR flows.

inputs:
    is-tagged:
        description: "Is this for the release of a tagged version? This action will wait for the tag to exist if so."
        required: true
outputs:
    version:
        description: "Version to deploy"
        value: ${{ steps.output-version.outputs.version }}

runs:
    using: "composite"
    steps:
        - name: Get version, release PR branch
          if: inputs.is-tagged == 'false' && startsWith(github.head_ref, 'release-please--')
          env:
            GITHUB_HEAD_REF: ${{ github.head_ref }}
          run: echo "VERSION=$GITHUB_HEAD_REF" >> $GITHUB_ENV                  
          shell: bash

        - name: Get version, perform release of tag
          if: inputs.is-tagged == 'true'
          run: |
            VERSION="$(tr -d '\n\r' < version.txt)"
            if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              echo "::error::Invalid version format in version.txt: '$VERSION'"
              exit 1
            fi
            echo "VERSION=v${VERSION}" >> $GITHUB_ENV
          shell: bash
          
        - name: Wait for ref to exist
          if: inputs.is-tagged == 'true'
          run: ./.github/workflows/scripts/wait-for-ref.sh "${VERSION}"
          shell: bash
          
        - name: Fail if no version set
          if: !env.VERSION
          run: exit 1
          shell: bash  

        - name: Output version
          id: output-version
          run: echo "version=${VERSION}" >> $GITHUB_OUTPUT  
          shell: bash
